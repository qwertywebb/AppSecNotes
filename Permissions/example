# Посмотреть права пользователя:
sudo -l

# Посмотреть какие файлы может запускать юзер с правами суперпользователя (SUID):
Если найден SUID-файл, и он уязвим, можно получить права root через эту программу.
find / -type f -perm /4000 2>/dev/null
find / -user root -perm /4000
