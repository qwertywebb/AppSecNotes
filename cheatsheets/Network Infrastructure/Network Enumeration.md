# Проверка TCP и UDP открытых портов.
netstat -an # Все соединения в числовом виде
netstat -l # Прослушиваемые порты
netstat -tp # Текущие соединения
netstat -tulnp # tcp + udp в числовом виде + процессы

# Проверка ip и mac-адресов хостов, которые коммуницировали с текущей системой
arp -a