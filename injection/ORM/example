# ORM Injection

# Frameworks and ORM Injection Testing

1. Laravel
- Eloquent ORM
# whereRaw(), DB::raw()
2. Ruby on Rails
- Active Record
# where("name = '#{input}'")
3. Django
- Django ORM
# extra(), raw()
4. Spring
- Hibernate
 # createQuery() with concatenation
5. Node.js
- Sequelize
# sequelize.query()